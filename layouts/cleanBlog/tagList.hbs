<div class="col-md-3">
  {{#each tags}}
    <div class="post-preview">
      <a href="/tags/{{this.urlSafe}}"">
        {{@key}}
       </a>
      {{#if this.count}}
        <span class="post-meta">{{this.count}} articles</span>
      {{/if}}
    </div>
  {{/each}}
</div>
